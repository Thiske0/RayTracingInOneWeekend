[package]
name = "ref-builder"
version = "0.1.0"
edition = "2018"

[dependencies]
gpu-builder = { path = "../gpu_builder" }

[target.'cfg(not(target_os = "cuda"))'.dependencies]
cust = { path = "../../../Rust-CUDA/crates/cust" }
once_cell = "1.21.3"